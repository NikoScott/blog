{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% form_theme contact_form 'bootstrap_5_layout.html.twig' %}


{% block body %}

    <div class="mt-5 p-5 col-8 mx-auto align-items-center">

        {# <h3>The contact has the following errors</h3>
        <ul>
            {% for error in errors %}
                <li>{{ error.message }}</li>
            {% endfor %}
        </ul> #}

        {# Afficher les msg flash#}
        {% for msg in app.flashes('confirmation') %}
            <div class="alert alert-success text-center">
                {{ msg }}
            </div>
        {% endfor %}
    
        {{ form(contact_form, {attr: {'novalidate': 'novalidate'}}) }}

    </div>

{% endblock %}
